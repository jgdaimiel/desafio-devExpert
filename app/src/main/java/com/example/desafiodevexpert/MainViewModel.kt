package com.example.desafiodevexpert

import androidx.lifecycle.ViewModel
import retrofit2.Retrofit

class MainViewModel: ViewModel() {
   /* var state = UIState()

    init {
        viewModelScope.launch {
            //state = UIState(Retrofit)
        }
    }*/
}